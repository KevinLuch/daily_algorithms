// Make a function using the && operator

// **************************************************************

function and(a, b) {
	if (a && b){
		return(true);
	}else{
		return(false);
	}
}
// **************************************************************