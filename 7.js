// Create a function that finds the maximum range
// of a triangle's thrid edge, where the side lengths are all
// integers
// **************************************************************

function nextEdge(side1, side2) {
	return (side1 + side2) - 1
}

// **************************************************************

const nextEdge_v2 = (side1, side2) => side1 + side2 - 1;

// **************************************************************