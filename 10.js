// Create a function that takes length and width and finds
// the perimeter of a rectangle
// **************************************************************

function findPerimeter(length, width) {
	perimeter = (length + width) * 2
	return perimeter 
}

// **************************************************************